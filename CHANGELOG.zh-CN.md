# 更新日志

[English](CHANGELOG.md)

以下记录近期发布的版本。Git 标签与 GitHub Release 现已和这些条目同步；历史条目继续保留原始发布提交链接。

## 0.1.44 - 2026-09-11

- 支持各渠道回传助手生成的文件，并可用 `/export` 导出当前会话；文件输入沿用 Chat 的准入规则，减少同一文件重复发送。
- 新增 `/menu`、Agent 预设和推理等级选择，支持渠道通过卡片处理命令、审批和问答；优化关联操作与任务状态提示，`/help` 统一使用纯文本。
- 新增断线、重启后的文字结果补发，可用 `/delivery` 查看并手动重试，不重新执行任务。送达未知的结果不会自动重发；手动重试可能重复，文件需在原会话查看。
- 修复钉钉部分卡片按钮无响应、新版 DSH 无增量回复，以及停止后已有正文被覆盖的问题；改善重启后 `/stop` 的状态提示。
- 继续支持 DSH 0.1.2-rc.1、0.1.5-rc.1 和 0.1.5-rc.2。升级后请重启 DSH 并刷新页面。

## 0.1.43 - 2026-09-11

- 新增对 DSH 0.1.5-rc.2 的支持，继续兼容 0.1.2-rc.1 和 0.1.5-rc.1。
- 升级后请重启 DSH 并刷新页面。

## 0.1.42 - 2026-09-10

- 支持 DSH 0.1.2-rc.1 和 0.1.5-rc.1；修复新版 DSH 中新建会话、切换工作区和恢复会话时的问题。
- 修复 IM 工具审批无法显示操作参数、需要转到网页处理的问题。
- 改善历史会话恢复：无法识别的个别记录不再阻断其他会话恢复，避免历史入口被错误清除。
- 修复窄屏下账号新增和设置弹窗被聊天区域遮挡的问题。
- 升级后请重启 DSH 并刷新页面。


## 0.1.41 - 2026-09-10

- 新增每账号独立的私聊、群聊命令开关。关闭命令后仍可正常聊天；私聊继续遵循原有准入规则。
- 在 IM 中管理会话、工作区、模型和推理等级，查看历史、改名、分叉，以及停止任务、补充指令和管理队列；`/help` 同时展示当前可用的 Chat 扩展命令。
- 优化账号设置：渠道卡片铺满内容区，账号设置通过弹窗修改，接收消息开关移到账号行；支持选择 Agent 预设，并修复添加工作区无响应的问题。
- 修复 `/new` 后旧会话在网页无法直接打开，以及切换工作区后新会话不回传回复的问题。新建会话沿用所选工作区和账号配置，旧会话继续保留。
- 命令回复补充清晰的结果、当前配置和下一步操作，列表标记归档及占用状态。支持跟随网页保存的中英文语言选择。
- 修复钉钉命令回复换行被合并的问题；AI 卡片创建失败时以纯文本保留回复换行。
- 升级后请重启 DSH 并刷新页面。

## 0.1.40 - 2026-09-10

- 修复 #12：使用 `/new`、`/clear` 或切换工作区后，旧会话仍保留在渠道列表，可继续打开、改名和归档，不影响当前聊天使用的新会话。
- 升级后，自动找回本机仍有聊天记录且能够识别所属渠道的历史会话，并保留原有归档状态。
- 会话名称跟随 DSH 标题更新，手动设置的名称不会被自动覆盖；新会话默认使用首条消息或附件名称。
- 修复归档、删除失败时会话入口消失，以及已删除会话仍残留在列表中的问题。
- 升级后请重启 DSH 并刷新页面。

## 0.1.39 - 2026-09-09

- 移除未使用的旧输入框组件，保留当前宿主输入入口。

- 统一设置页标题、说明与操作布局，标题及版本不再被维护按钮挤压；适配原生 DSH 设置内容区，无需安装 Codex UI。
- 账号列表使用紧凑双行渠道条目，左右列表和详情保持等宽布局；修复原生设置弹窗中盒模型导致的横向溢出。

## 0.1.38 - 2026-09-08

- 修复 #11：设置页管理 API 迁至 `/api/dsh-im-connect`，遵循其他 DSH 插件的 REST 前缀。接口通过 DSH 公开的 `connection.requestRejection`，反向代理下保留可信 Host、Origin 和绑定访问地址的浏览器 Cookie 校验。本机请求也必须登录；认证不可用时安全关闭并提示稍后重试或升级。
- 保留账号、模型、扫码与会话管理路由；前端显式携带同源凭据，禁止缓存管理数据，并增加真实宿主认证及前端调用回归测试。

- 为微信、企业微信、钉钉、飞书、Lark、QQ 和 Telegram 接入原生图片消息，复用 DSH Chat 的当前会话模型判断、附件校验与持久化；修复账号默认模型覆盖 Chat 会话模型的问题。
- QQ 图片下载统一使用连接时 DNS 校验的安全下载器，保留整条消息大小预算、取消与超时；支持管理员按账号配置额外的精确图片主机，避免为区域 COS/OSS 地址反复修改代码。
- 钉钉图片下载复用账号级 access token 缓存，合并并发取 token 请求，并在到期、停止或重新连接时失效。
- 微信图片下载区分失败原因，对瞬时网络/服务端故障进行有限重试；失败时不把图片附言降级为纯文字指令，停止后不继续重试或投递旧消息。
- 增加图片安全、顺序、生命周期、宿主图片准入和附件持久化回归测试；真实渠道与视觉模型联调状态单独记录，不以自动化夹具替代。

## 0.1.37 - 2026-09-07

- 撤回尚未开发完成的主动投递功能，包括账号投递设置、`im-send`、Agent 工具及投递 HTTP 接口；保留原有 IM 对话回复和插件更新功能。

## 0.1.36 - 2026-09-07

- 将 DSH Webserver 的 peer 与开发依赖统一为 `0.1.2-rc.1`，使 CI 能够解析该包的依赖图。

## 0.1.35 - 2026-09-07

- 新增独立的插件内检查更新能力：仅在已验证的 DSH 更新服务可用时自动更新，其余环境提供与 Profile 对应的手工更新命令。
- 更新按钮图标不再依赖 `react-dom/client`，避免宿主未注册该模块 id 时客户端无法加载。

## 0.1.34 — 2026-09-03

- 支持 DeepSeek Harness `0.1.2-rc.1`。

## 0.1.33 — 2026-09-03

- 在 IM 助理设置标题旁新增 GitHub 和 Issues 入口，并与归档管理器统一图标尺寸、按钮尺寸、交互状态和响应式布局。
- 为插件实际使用的 DSH 客户端与 Agent 模块声明 `>=0.1.0-rc.5 <0.2.0` 的 peer 依赖；可发布的 DSH 开发模块统一锁定至 `0.1.2-alpha.5`，并升级 Cordis、Schemastery 开发基线。

## 0.1.32 — 2026-09-02

- 账号工作区确实变化时重建其 IM 会话映射，保留宿主历史记录，并保证下一条入站消息在新工作区创建会话。Windows 等价路径不重置会话；Linux 下仅大小写不同的路径会正确触发重置。
- 账号更新与扫码绑定现在会等待配置落盘和渠道重载完成才报告成功。保存期间的取消控件保持锁定，并在宿主设置页之前拦截 Escape。

发布包：[`@michengai/dsh-im-connect@0.1.32`](https://www.npmjs.com/package/@michengai/dsh-im-connect/v/0.1.32)。

## 0.1.30 — 2026-09-01

- 修复账号切换模型时沿用旧 Low、Medium 或 High 推理等级的问题；无推理等级模型不再携带该字段，并会自动清理旧版本已保存的无效值。

发布包：[`@michengai/dsh-im-connect@0.1.30`](https://www.npmjs.com/package/@michengai/dsh-im-connect/v/0.1.30)。

## 0.1.29 — 2026-09-01

- 补齐多账号设置全流程的宿主语言本地化，覆盖账号配置、选择器、状态、操作、空状态、确认提示与可执行的服务端错误。
- 自动生成的账号名现在会随宿主语言显示，无需迁移已有数据；自定义账号名保持原样。
- 修复狭窄账号详情栏的英文操作按钮布局：精简连接操作文案并保持单行对齐，极窄宽度下按整颗按钮换行。

发布包：[`@michengai/dsh-im-connect@0.1.29`](https://www.npmjs.com/package/@michengai/dsh-im-connect/v/0.1.29)。

## 0.1.28 — 2026-09-01

- 微信、企业微信、QQ、钉钉、飞书、Lark 与 Telegram 现可接入多个相互隔离的账号；每个账号独立保存工作区、模型、权限、私聊准入、凭据、白名单与会话状态。
- 重新设计 IM 助理设置：按渠道添加账号、记住账号选择、独立编辑账号配置，并补齐空状态、按需二维码与桌面窄窗口响应式布局。
- 加强公开私聊安全边界：未批准用户不能审批本机工具调用；旧版审批接口在账号不明确时安全失败；账号重载会主动取消仍在等待的工具审批和结构化问题。

发布包：[`@michengai/dsh-im-connect@0.1.28`](https://www.npmjs.com/package/@michengai/dsh-im-connect/v/0.1.28)。

## 0.1.27 — 2026-08-31

- 将 DSH 工具审批和结构化用户问题路由到发起任务的 IM 会话，通过文字回复完成审批、单选、多选和自定义回答。
- 同时兼容 DSH 当前及旧版交互契约；同一会话的提示按顺序处理，群聊只允许发起用户作答。
- 加强提示发送与取消处理，避免分片发送、投递失败、任务中止、会话重置或插件卸载后留下仍可响应的过期交互。

发布包：[`@michengai/dsh-im-connect@0.1.27`](https://www.npmjs.com/package/@michengai/dsh-im-connect/v/0.1.27)。

## 0.1.25 — 2026-08-27

- 修复侧栏包装逻辑：官方工作区树继续使用宿主翻译器，频道页签只显示一个，注册表标签随语言实时刷新且不会残留订阅。
- 完全移除外部二维码兜底。配对数据只在本地生成二维码，本地生成失败时安全终止配对。
- 删除渠道时立即撤销运行时授权，并清理已存凭据以及微信身份、上下文和二维码状态。
- 将 IM 助理多字段设置改为原子更新，非法请求不会再部分修改当前配置。
- 新增回归测试和 Pull Request CI，自动执行测试与打包验证。

发布包：[`@michengai/dsh-im-connect@0.1.25`](https://www.npmjs.com/package/@michengai/dsh-im-connect/v/0.1.25)。

## 0.1.24 — 2026-08-26

- 恢复 IM 助理设置中内置权限预设的中文显示，不再暴露缺失的 `preset.*` 翻译键。
- 完全访问风险确认继续使用宿主管理的权限翻译域，显示名称则改用 IM Connect 自己的中英文词典。
- 将中英文 README 的更新日志入口从底部移到顶部导航。

发布包：[`@michengai/dsh-im-connect@0.1.24`](https://www.npmjs.com/package/@michengai/dsh-im-connect/v/0.1.24)。

## 0.1.23 — 2026-08-23

- 新增中英文更新日志，展示最近五个发布版本。
- 在中英文 README 中加入更新日志入口，并将日志纳入 npm 包。

发布包：[`@michengai/dsh-im-connect@0.1.23`](https://www.npmjs.com/package/@michengai/dsh-im-connect/v/0.1.23)。

## 0.1.22 — 2026-08-23

- 刷新审批状态处理。
- 放行当前登录流程需要的 QQ 连接器扫码器。

发布提交：[`47d1936`](https://github.com/MichengAI/dsh-im-connect/commit/47d1936)。

## 0.1.21 — 2026-08-23

- 信任已经校验的企业微信群回调。
- 加强 IM 频道运行时处理。

发布提交：[`be07643`](https://github.com/MichengAI/dsh-im-connect/commit/be07643)。
