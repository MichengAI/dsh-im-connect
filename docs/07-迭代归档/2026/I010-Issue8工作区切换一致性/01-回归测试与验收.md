# I010 回归测试与验收

更新时间：2026-09-02 07:00（Asia/Shanghai）

## 问题复现

基线执行 `npm test`，原有 163 项测试全部通过，说明旧测试未覆盖 Issue #8。

新增回归测试后执行相关测试，共 42 项，其中 39 项通过、3 项按预期失败：

- 路由层缺少工作区变化后的旧映射重置能力
- 账号设置接口同步返回，没有等待旧会话处理完成
- `PairingHub` 在账号保存完成前已经报告 `success`

## 修复后验证

- 聚焦回归：45 项全部通过
- 全量回归：168 项全部通过，0 失败、0 跳过
- TypeScript 编译和客户端产物复制由 `npm test` 中的 `npm run build` 完成
- `channels.json` 行为测试确认账号新 `cwd` 已真实落盘
- 路由行为测试确认下一条消息使用新目录创建新 sessionId
- 等价路径测试确认格式差异不会误清会话
- 扫码测试确认保存期间为 `saving`，保存成功后才进入 `success`，保存失败保持 `failed`

## 影响与回滚

工作区变化会让该账号的后续消息创建新会话；旧 Host 会话日志不删除，但旧聊天映射会解除。若需回滚，撤销 I010 对以下模块及测试的修改即可：

- `src\manager.ts`
- `src\engine\gateway.ts`
- `src\engine\router.ts`
- `src\channels\qr\hub.ts`
- `src\channels\qr\shared.ts`
- `client.js`
- `tests\assistant-settings.test.mjs`
- `tests\gateway-access.test.mjs`
- `tests\router.test.mjs`
- `tests\qr-hub.test.mjs`
- `tests\bind-modal.test.mjs`
