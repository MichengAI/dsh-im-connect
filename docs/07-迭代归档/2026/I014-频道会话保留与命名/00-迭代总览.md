# I014 频道会话保留与命名

状态：已完成
负责人：Codex
优先级：P1
更新时间：2026-09-09 22:49（Asia/Shanghai）

## 目标与边界

修复 [Issue #12](https://github.com/MichengAI/dsh-im-connect/issues/12)：`/new` 只切换当前聊天绑定，旧会话仍可在频道下访问和归档。完成范围为本地代码、构建产物与自动化验证，不含发布或真实 IM 联调。

## 实现与任务验收

- 原有聊天键仍指向当前会话；旧记录以 `history:<sessionId>` 独立保存，兼容旧版平面 JSON。
- 启动通过宿主 `sessionPersistence.list()` 补回可解析 IM ID 的孤立日志索引，保留归档状态，不自动设置为当前会话。
- 历史打开按指定 ID 恢复；恢复失败返回失败，不创建空记录或轮换当前绑定。
- 历史改名、归档与删除按会话 ID 操作，不清除同一聊天的新会话。宿主归档失败不再继续移除列表映射。
- 宿主仅提供活会话列表时不据此清除历史；宿主卸载仍有日志的会话时保留索引。
- 命名采用首条原始消息兜底，同步宿主标题事件；手动标题不被自动结果覆盖。恢复时读取已保存的标题事件，列表优先展示宿主现有标题。
- 控制字符、终端转义、方向控制符清理，兜底标题最多 60 UTF-8 字节，按字素截断。

## 验证与遗留事项

- 新增回归先验证失败：`/new` 仅剩一条记录，孤立日志没有恢复索引。
- `npm test`：406 项，391 通过、15 跳过、0 失败；包含 TypeScript 构建。
- 覆盖历史保留、当前绑定隔离、旧格式重启、迁移备份、归档恢复、宿主列表缺失、标题优先级与 Unicode。
- 跳过项沿用测试环境条件，未宣称真实 Host 合同测试全部通过。
- 真实 DSH 界面、手机渠道收发及归档管理器联调未执行，发布前由维护者验证。
- 孤立日志若不含可识别 IM ID，无法自动重建渠道关联；宿主没有持久化服务时跳过恢复。未加载到宿主标题的旧记录先显示聊天 ID，打开后同步日志标题。

## 参考

参考 [dsh-im 首条消息命名](https://github.com/xmanrui/dsh-im/blob/main/src/channels/shared/session-title.mjs)、[命名调用条件](https://github.com/xmanrui/dsh-im/blob/main/src/channels/shared/workspace-session.mjs)与[宿主标题装饰](https://github.com/xmanrui/dsh-im/blob/main/plugin-src/host/session-title-prefix.mjs)。本项目频道已有分组，不重复添加渠道前缀；本地兜底不调用宿主重命名，以免锁定自动命名。

## 恢复与交付

首次扩展历史索引前保留 `sessions.json.before-history.json`（实际前缀取配置中的映射文件名），已有备份不覆盖。回退前停止插件并另行备份当前映射，再恢复升级前映射及旧代码；升级后新增会话的日志不会因此删除，但旧版可能没有其列表入口。宿主日志、账号配置未迁移或删除。

关联提交：本次 fix: preserve channel session history and synchronize titles（见 Git 历史）。用户已授权提交及本地安装验证；未推送、未发布、未回复 Issue。

阅读顺序：本总览 → `src/engine/session-store.ts` → `src/engine/router.ts` → `src/engine/session-title.ts` → 对应测试。
