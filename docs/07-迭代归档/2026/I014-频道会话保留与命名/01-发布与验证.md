# 0.1.40 发布与验证

更新时间：2026-09-10 00:03（Asia/Shanghai）

## 验证依据

- 本机 Node.js 环境执行 `npm test`，设置三个真实宿主包合同路径：416 通过、0 失败、0 跳过。
- 核对真实 `dsh-session-title@0.1.2-rc.1` 的类型与事件写入，来源为 `user / provider / fallback`。
- 中英文 CHANGELOG 均含 0.1.40 条目，生成的 Release 说明中文在前、英文在后。
- `npm pack --dry-run --ignore-scripts`：262 个文件，包含编译产物、源码和双语说明。
- `git diff --check` 通过，去除本次构建产生的纯行尾噪音。

## 发布状态

发布目标：npm `@michengai/dsh-im-connect@0.1.40` 与 GitHub `v0.1.40`，沿用标签工作流。发布完成后核对 npm `gitHead`、标签提交与 Release 双语正文，并安装 npm 正式包到本机 `web` Profile；不额外备份旧 npm 包。

## 验证边界

用户明确接受本次先发布，真实界面和手机联调保留为未验证。浏览器自动化当前报错 `Codex auth token is unavailable`；未发送真实渠道测试消息，未宣称归档管理器 UI 或运行中会话时序通过。安装后需要重启 DSH 并刷新页面加载新版。

后续真人验收：`/new → 打开旧会话 → 手动改名 → 归档/恢复 → 重启`，确认旧记录保留、手动标题稳定且手机当前绑定不被旧会话操作改变。
