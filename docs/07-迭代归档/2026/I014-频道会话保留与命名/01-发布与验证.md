# 0.1.40 发布与验证

更新时间：2026-09-10 00:24（Asia/Shanghai）

## 验证依据

- 本机 Node.js 环境执行 `npm test`，设置三个真实宿主包合同路径：416 通过、0 失败、0 跳过。
- 核对真实 `dsh-session-title@0.1.2-rc.1` 的类型与事件写入，来源为 `user / provider / fallback`。
- 中英文 CHANGELOG 均含 0.1.40 条目，生成的 Release 说明中文在前、英文在后。
- `npm pack --dry-run --ignore-scripts`：262 个文件，包含编译产物、源码和双语说明。
- `git diff --check` 通过，去除本次构建产生的纯行尾噪音。

## 发布状态

- npm `@michengai/dsh-im-connect@0.1.40` 已发布，`latest` 为 0.1.40，`gitHead` 为 `4697b4d6699b6c9eb29ef86600d9d9e3e7be7203`。
- GitHub `v0.1.40` 已公开发布，正文为中文在前、英文在后，与双语 CHANGELOG 对齐。
- 本机 `web` Profile 已安装 npm 正式包 0.1.40；未额外备份旧 npm 包。用户重启后确认钉钉旧会话可以正常打开。
- Issue #12 已回复版本、升级方式与验证边界，并关闭。
- 发布期间曾因工具调用报错暂停：Actions 取消前 npm 上传已完成，GitHub Release 暂转草稿；尝试回调 npm latest 返回 E403，未生效。后台核对发现 PUA 在工具调用与结果之间插入消息，用户确认该问题归属 PUA 后明确授权继续发布；随后恢复 GitHub Release 公开状态，未重复上传 npm 包。

## 验证边界

用户明确接受继续发布；钉钉旧会话打开已由用户实测确认，其他组合联调仍待验证。浏览器自动化当前报错 `Codex auth token is unavailable`；未发送真实渠道测试消息，未宣称归档管理器 UI 或运行中会话时序通过。安装后需要重启 DSH 并刷新页面加载新版。

后续真人验收：`/new → 打开旧会话 → 手动改名 → 归档/恢复 → 重启`，确认旧记录保留、手动标题稳定且手机当前绑定不被旧会话操作改变。
