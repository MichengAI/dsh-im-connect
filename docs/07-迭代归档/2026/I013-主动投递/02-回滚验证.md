# 主动投递回滚验证

更新时间：2026-09-07 16:14（Asia/Shanghai）

## 范围

撤回 `9028bac` 中未完成的主动投递功能，保留同笔提交中的插件更新、后续兼容和发布流程修复。用户已授权提交、推送和发布 `v0.1.37`，并下架上一版 `v0.1.36`，不改写实现提交历史。

移除账号投递开关和目标编辑、`im-send` Skill、四个 Agent 工具、投递 HTTP 接口、七渠道 `sendProactive`、专用依赖和编译产物。原有 `send` 回复、审批消息和回复兜底路径保留。

没有访问或迁移真实账号数据；既有 `channels.json` 中可能存在的投递字段不再被执行。原实现仍保存在 Git 历史中，后续开发可按需参考。

## 检查

- `npm test`：构建成功，177 项测试通过，0 失败；包含原有回复链路及插件更新测试。
- 回滚专用检查：确认旧投递路由返回 404、原有账号列表仍可读取；确认发布包没有遗留投递模块。
- 差异复核：投递独占后端文件与 `8d5309e` 一致；插件更新实现、测试和工作流与回滚前一致。

回滚提交：由 `v0.1.37` 标签定位。发布状态以对应 GitHub Release、工作流和 npm Registry 为准；已安装的 Host 仍需升级并重启才能加载回滚版本。
