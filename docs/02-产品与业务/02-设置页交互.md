# 设置页交互

更新时间：2026-08-18 00:10（Asia/Shanghai）

视觉稿：[01-设置页-IM频道.png](../06-设计资源/01-设置页-IM频道.png)  
实拍：[settings-channels.png](../../assets/screenshots/settings-channels.png)

使用者操作步骤见 [使用说明](./04-使用说明.md)。本页只约定设置页结构和绑定方式。

## 入口

- 设置里的插件页签名称：**IM助理**
- 打开后是独立设置页，不是塞在「插件」列表里的一行开关

## 页面结构（按截图 1:1）

1. 顶部装饰：左右两条提示气泡 + 中间两个头像（吉祥物 / 用户）。第一期可先用静态装饰，不接真实对话。
2. 主标题：**IM 频道**
3. 副标题两行：
   - 配置 IM 频道，让本机助手接收来自钉钉、飞书等平台的消息。
   - 频道配置信息仅存储在本地，不会上传到云端。
4. 渠道卡片纵向列表，卡片之间留缝，深色圆角描边。

截图原文是「让千问办公接收」。本产品文案改成「本机助手」，**不要出现「千问办公」**。其余层级、对齐、按钮形态按截图还原。

## 卡片两种状态

### 未连接

- 左：平台图标
- 中：平台名 + 一行说明「通过 {平台} 机器人接收并回复用户消息」
- 右：白色描边按钮 **配置**

点击「配置」打开该渠道的接入流程（扫码或填凭据），不要跳到别的设置分类。

### 已连接

- 平台名右侧绿色标签 **已连接**
- 右侧不再显示「配置」，改为：
  - `...` 更多菜单（断开、重新接入、查看状态）
  - 绿色开关，打开=接收消息，关闭=暂停接收但保留凭据

截图中微信、企业微信即为此态。

## 渠道顺序

按视觉稿顺序排，并补上 QQ、Telegram：

1. 钉钉
2. 飞书
3. Lark（飞书国际版，同一适配器、不同域名，单独一张卡）
4. 微信
5. 企业微信
6. QQ
7. Telegram

Lark 与飞书在界面上是两张卡，实现上共用飞书适配器。

## 不在第一期做的界面

- 截图顶部那两条英文提示气泡的真实对话能力
- 渠道内的复杂统计看板
- 把 20+ 渠道塞进同一列表

## 配置弹窗

点击卡片「配置」或已连接菜单「重新接入」打开居中弹窗，不再在卡片内展开表单。

| 渠道 | 绑定方式 |
|---|---|
| 微信 | 仅二维码，可重新生成 |
| 飞书 | 仅扫码自动创建机器人，可刷新二维码 |
| Lark | 仅扫码配对，同飞书国际版域名 |
| 企业微信 | 快捷绑定（推荐）扫码，或手动填 Bot ID / Secret |
| 钉钉 | 扫码或手动。手动字段为 Client ID（原 AppKey）、Client Secret（原 AppSecret） |
| QQ | 扫码或手动。手动字段为 AppID / AppSecret |
| Telegram | 仅手动填 Bot Token |

群聊不用绑定，只需 @。私聊里，扫码用户自动放行；其他私聊用户会出现在设置页待批准列表。不再提供钉钉开放模式。

扫码成功后由本机保存凭据并启动渠道，弹窗关闭，卡片变为已连接。

## 助手模型

设置页顶部选择 IM 会话使用的工作区、权限和模型。保存后只影响频道会话，不改网页任务。未选择时，创建会话会提示先在本页设置。
