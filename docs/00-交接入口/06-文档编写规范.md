# 文档编写规范

更新时间：2026-08-16 17:02（Asia/Shanghai）

本仓库遵循根目录 `AGENTS.md` 第十一节。这里只列本项目补充约定。

1. 使用简体中文和 UTF-8。
2. 当前事实只写在 `02-当前状态.md`，未完成事项只写在 `03-待办与阻塞.md`。
3. 架构结论只写在 `docs\03-技术架构`，迭代总览只摘要并链接。
4. 新迭代编号取当前最大 `Ixxx` 加一，不回收。
5. 不创建 `docs\superpowers`、`docs\plans` 等工具默认目录。
6. 只在确实需要时加文档，禁止空模板。
