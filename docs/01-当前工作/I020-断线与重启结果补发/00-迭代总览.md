# I020 断线与重启结果补发

状态：进行中

负责人：Codex；维护者负责真实渠道验收

优先级：P1

更新时间：2026-09-11 11:15（Asia/Shanghai）

## 目标与边界

实现普通 IM 输入的文字结果恢复。先持久化 requestId、原会话和稳定收件标识，再提交宿主。结果按精确请求对应回合读取，不重新 prompt、不 resolveAgent、不执行工具或重放审批与文件。Chat 扩展命令、旧版已处理消息、整机停机期间未被渠道接收的消息不在本次范围。

## 已完成

- 独立 `deferred-delivery.json`，版本 1；原子落盘，发送前记录状态，记录写入失败不发送。重启时 sending 转为 unknown，默认不自动重试未知结果。
- 实时流式发送同样先记为未知，回合确认送达后才标为 sent。流式收口失败取消旧的自动文字兜底，避免再发一份；失败提示关联 `/delivery`。
- 固定分片计划和进度，单会话串行恢复，同回合多个请求合并投递；分片间重新检查账号、准入和当前绑定。
- 冷历史使用 Session Controller 的 follow 首帧及 page，首帧后立即关闭，不推进 prepared 会话的激活。最多 20 页、20000 条记录、30 秒读取信号；单回合文字超过 100 万字符时不自动恢复，保留网页原记录。
- 每 30 秒轮转检查最多 20 条。Telegram、飞书、企业微信声明稳定主动投递能力；钉钉、QQ、微信等待原聊天的新消息或显式 `/delivery retry` 刷新通道。渠道网络错误仍视为未知，不猜测失败后盲目重试。
- `/delivery` 查询最近 10 条，`/delivery retry <记录ID>` 显式补发。查询与补发绑定账号、聊天和原用户；入口继续先准入、再检查命令权限。中英文文案和帮助同步。
- 补发窗口 7 天、最多 1000 条；满额优先淘汰已送达记录，不静默淘汰窗口内未完成记录；全部为未完成记录时拒绝继续注入。新消息进入时清理过期记录。

## 验证

自动化覆盖请求写盘后重启、冷读取精确回合、防跨回合、发送前／发送中／回执落盘前故障、未知不重试、固定分片、同回合去重、准入撤销、停用、换绑、操作者隔离、命令权限、英文输出，以及实时离线后恢复。

三套实际宿主契约 `0.1.2-rc.1`、`0.1.5-rc.1`、`0.1.5-rc.2` 完整回归各 680 项通过、0 失败、0 跳过。新增真实 SessionHistoryController 分页测试核对首帧关闭不会调用 promote，观察句柄全部释放。这是自动化契约证据，不等于真人渠道验收。

## 待处理与阻塞

- 维护者验收：钉钉等待原聊天新消息后补发；企业微信主动配额、后台重连；流式送达未知后手动补发的手机呈现。
- 已随 0.1.44 推送并发布到 npm 与 GitHub Release；本轮未更新正式本机 Profile。真实渠道手机验收仍待维护者完成。
- 跨平台无统一送达凭证，无法保证绝对恰好一次。手动重试未知结果可能重复，命令说明已明确。

## 恢复与关联提交

关联提交：本次 `feat: recover deferred IM text deliveries`。回退上一插件版本可撤回新增能力，不修改宿主会话日志；新交付文件保留但旧版不读取。回退后继续运行产生的发送不受本版记录，重新升级时应先人工核对旧待补发记录，避免把回退期间的发送当作尚未发送。

阅读顺序：本总览 → README 断线与重启后的结果补发 → `src/engine/deferred-delivery.ts` → `src/engine/delivery-history.ts` → gateway 集成及对应测试。


## 交付审查补修

- 按实际文字／文件内容登记投递，不再用 tool-call 排除整条消息；无内容不计为成功投递。冷恢复也保留含工具调用消息的文字块，不包含工具参数或推理。
- 失败／停止的流式收口保留累计正文，再追加状态与关联命令，避免 Telegram 和钉钉 AI Card 的整条替换丢失部分答案。
- 提交宿主前失败进入 rejected；调用已开始但接收情况不明进入 unknown。两者均停止后台轮询，只有 unknown 可通过明确重试读取原结果，不重新注入请求。满额时可淘汰 rejected 终态，停用账号不重新激活它。
- 空正文恢复省略“补发结果”头部，仅发送结束状态。新增 rejected 状态提供中英文显示。

先复现五个失败用例，再完成修复。最终三个支持版本各 690 项通过、0 失败、0 跳过。新增渠道回调三路径、正文保留、混合工具文字、文件与空结果记账、拒绝记录重载与容量、停止轮询回归。手机端仍未复测。

关联提交：本次 `fix: preserve reply content and settle rejected deliveries`。rejected 是新增记录状态，52f5442 的旧读取器不认识它；若必须回退到该提交，须先对明确拒绝记录做终态格式兼容处理，不能直接让旧读取器打开新状态。回退到尚无补发功能的发行版不会读取该文件，宿主会话日志不受影响。


## 补发暂停语义与完整测试尝试

关联提交：本次 `fix: keep unresolved delivery retries paused`。

空结果正常完成使用 ended／已结束状态，Telegram 和飞书对应完成类表情；通知仍解释没有可投递结果。手动重试不提前将 unknown／blocked 改为 waiting，冷读取查不到结果或抛错时保留暂停，并提供中英文“本次未发送”回执。注入失败后的落盘异常独立记录，保留原始准入错误并继续清理进度；落盘失败记录释放 active，但在当前进程隔离，禁止自动或手动发送。磁盘写入失败无法保证跨重启保存新的状态，重启只能以最后成功持久化的记录为准。移除钉钉成功点击的逐条 info 日志。

新增回归先复现：手动重试缺失／异常历史、落盘失败掩盖原始错误及阻断清理、空结果已取消标签。最终 `0.1.2-rc.1`、`0.1.5-rc.1`、`0.1.5-rc.2` 各 697 项通过、0 失败、0 跳过。覆盖现有全部渠道测试、请求注入、真实宿主历史读取契约、模拟引擎重启恢复、失败与权限边界；不等同于启动完整宿主后的端到端通过。

完整进程端到端尝试：

1. 创建新的隔离目录 `C:\Users\YUJIYU\AppData\Local\Temp\dsh-im-delivery-e2e-daa8c28d`，对当前代码执行 npm pack，通过 DSH CLI 安装到该目录的 web Profile。首次安装被 protobufjs 构建脚本策略阻止，执行 `dsh plugin --profile web install --ignore-scripts` 后安装完成。未改变正式 Profile。
2. 隔离安装的 249 个 lib 产物全部与当前工作区 SHA256 一致。宿主 CLI 版本为 0.1.5-rc.2。模型与 Telegram HTTP 模拟器仅监听回环地址，使用测试数据；未向真实机器人发送消息。
3. 计划通过独立进程启动 `dsh web --no-open --host 127.0.0.1 --port 10961`，仅该进程设置隔离 DSH_HOME 和测试传输。自动审批在命令执行前拒绝，返回 `blocked by policy`，没有进一步原因。宿主未启动，因此新消息→真实 Agent→回复→重启恢复，以及真实设置页、手机卡片验收均未完成，不记为通过。
4. 已停止本轮启动的模拟服务。隔离安装目录、打包文件及安装日志保留，供解除环境限制后续测。未启动或重启用户正式宿主，未安装到正式 Profile，未推送或发布。

上述为首次尝试的历史记录。后续直接前台启动成功，当前已无该启动阻塞；结果见 [01-隔离宿主端到端复测.md](01-隔离宿主端到端复测.md)。真实渠道手机验收仍由维护者负责。


## 隔离宿主复测与冷会话停止补修

直接前台启动 DSH 0.1.5-rc.2 成功。真实宿主、真实网页、回环模型与 Telegram API 模拟器完成准入、首条回复、命令、工作区切换、旧会话网页打开、重启索引与绑定保留、未知结果手动补发及模型不重跑验证。不能据此前一次 `blocked by policy` 判断整个环境无法测试。

复测发现重启后冷会话 `/stop` 的 `session/not-found` 原样报错。现仅在列表明确确认该会话空闲时回复“当前没有正在执行的任务”，不激活 Agent；运行中、状态未知和其他错误继续抛出。中英文同步，回归先红后绿；隔离宿主重启后实测停止回复正常，随后普通聊天能恢复并回传。

当前回归共 698 项：启用 0.1.5-rc.2 宿主契约后 697 通过、0 失败、1 跳过；为剩余导出契约补齐环境后单独运行 1 项通过。默认不配置宿主路径时为 673 通过、0 失败、25 跳过。本轮新增补修没有重跑另外两个旧版本矩阵，不将上轮 697 项三版结果算作本轮结果。

本次补修纳入 `fix: handle stop requests for idle cold sessions`，未推送、未发布；仅更新隔离测试安装，不改正式 Profile。真实手机呈现、完整空回合以及增量流中断仍未在本次进程测试中验收。


## 0.1.44 发布补修

`/stop` 的 session/not-found 仅在宿主列表明确为空闲时回复空闲；其他状态、条目缺失与查询异常转为中英文可操作提示，不再透传内部英文错误，其他错误不改变处理。

专项复测定位到新版宿主以 agent/assistant-stream 发布临时增量，而插件只订阅旧 assistant/chunk。新增事件适配复用原有绑定、准入和冷回合检查，过滤推理与非文本，不改变旧宿主路径。真实 0.1.5-rc.2 进程加本地 SSE/Telegram 模拟器已观察到两次增量编辑，间隔 1.2 秒；随后 /stop，同一条原消息保留两段正文，停止提示送达。前轮“没有有效覆盖”已由此次专项验证补齐；仍不代表真实手机渠道验收。

本轮 699 项回归通过、0 失败、0 跳过（0.1.5-rc.2 契约全部启用）。已发布 0.1.44，公开说明包含自 0.1.43 以来的文件、命令交互与文字补发能力，只描述用户相关变化。发布提交为 `afde399`，三个支持版本远程 CI 全部通过。npm 的 latest 为 0.1.44，gitHead 与发布提交一致；GitHub Release 双语正文与两份 CHANGELOG 提取结果一致。
