# 0.1.47 发布验证

更新时间：2026-09-13 21:09（Asia/Shanghai）

## 范围与结果

账号真实接口诊断、状态映射守卫、旧后端接收开关保护、双语文档与发布流程加固。发布提交：`3844bf018e3e3da8727c9b3cfe2700970cf9c57e`，标签 `v0.1.47`。npm 与 GitHub Release 已发布，远程验证结果见末尾。

本机 Windows、Node.js 24.20.0、DSH 0.1.5-rc.2。全量 npm test 共 774 项，773 通过、0 失败、1 项导出契约路径未配置而跳过；补齐真实导出依赖路径后，该项单独通过。其他宿主契约直接使用已安装 rc.2 包。三版契约矩阵由发布工作流继续验证，不将本机单版本结果扩展为三版通过。

构建后 lib 与 client.js 无差异。发布包安装至全新隔离 Profile，257 个 lib 产物逐一 SHA256 与工作区相同。安装首次触发 protobufjs 构建脚本策略，使用既有 install --ignore-scripts 流程完成，没有改变正式 Profile。

## 端到端

真实 DSH 进程、插件管理接口、账号状态持久化、Agent、Session Controller 与网页界面；模型和 Telegram HTTP 平台为本地模拟服务。仅宿主进程加载测试传输，将 Telegram 请求重定向至回环地址；没有向真实账号发测试消息。

| 场景 | 结果 |
| --- | --- |
| 网页配置账号 | 创建测试工作区、选择本地模型、绑定模拟 Telegram，成功 |
| 接收意图 | 账号行切换接收后，设置页提示暂停；诊断不会重新启用接收 |
| 暂停时诊断 | getMe、getWebhookInfo 各一次；无额外 getUpdates、无聊天发送，见 [请求证据](08-0.1.47验证证据/diagnostic-calls.json) |
| HTTP 401 | 显示凭据缺失或失效及 HTTP 401，不误报网络 |
| Webhook 冲突 | 第二项失败并给出建议；不返回原 Webhook 地址、不自动删除 |
| 中英文 | 通过宿主语言设置切换，诊断区域无中文词条残留；宿主提供的 Agent 预设名称保留原标签 |
| 桌面与窄屏 | 1440×1000 与 390×844 实际渲染；窄屏弹窗宽 366、左边距 12，诊断按钮命中未被遮挡，可滚动查看完整报告 |
| 准入与消息往返 | 未批准用户不调用模型；批准后经真实 Agent 完成回复 |
| 会话操作 | /rename、/new、/sessions 成功；新建后旧会话可重新打开 |
| 重启恢复 | 索引与旧会话保留；按重启前实际计数确认无额外模型调用、无重复消息，重启后诊断及新消息回复成功 |

截图：[桌面冲突提示](08-0.1.47验证证据/diagnostic-desktop.png)、[窄屏完整报告](08-0.1.47验证证据/diagnostic-mobile.png)、[英文界面](08-0.1.47验证证据/diagnostic-english.png)。[重启结果](08-0.1.47验证证据/restart-results.json)。

测试准备修正：首次重启断言错误假设模型只调用一次，实际宿主已经调用两次；重新保存重启前实际调用与消息基线、再次重启后逐项比较通过。此失败属于测试基线假设错误，未修改产品代码。浏览器 CLI 在调整视口后需先将按钮滚入视口再点击；命中检查通过。

## 发布前检查与未覆盖

双语发行说明按 package 版本提取成功，差异检查通过。给渲染夹具补充闭包依赖注释，为旧组件切片增加边界断言，未重构产品组件。

真实钉钉、QQ、飞书、Telegram 平台外呼，以及企业微信服务端是否接受 diagnostic_ 前缀 ping 仍未验收。本轮本地模拟端到端不能代替真实平台或手机验收。旧后端缺字段路径由渲染回归覆盖，未另启动旧版 Host 页面。升级需重启 DSH 并刷新页面。

隔离目录保留在 `C:\Users\YUJIYU\AppData\Local\Temp\dsh-im-release-0147-20260913` 作为必要证据。Cookie 与测试凭据不复制进仓库；测试服务在结束时停止。回退可安装 0.1.46 并重启宿主，不涉及用户消息或会话数据迁移。

## 发布完成

- [发布工作流 34758856989](https://github.com/MichengAI/dsh-im-connect/actions/runs/34758856989) 全部成功：三个宿主版本各 774 项、773 通过、0 失败、1 项导出契约未配置跳过。本机已补齐该导出契约单项通过。main 测试工作流 34758854844 同样成功。
- npm 官方 registry.npmjs.org 的 latest 为 0.1.47，gitHead 与上述发布提交一致；下载公开 tarball 并校验 SHA512 完整性通过。
- 公开包 lib 与 client.js 共 258 个文件核验：257 个字节完全相同；lib/client.js 仅 CRLF/LF 差异，规范化换行后内容一致。此差异来自 Windows 本机构建与 Linux 发布，不是代码遗漏。
- [GitHub Release v0.1.47](https://github.com/MichengAI/dsh-im-connect/releases/tag/v0.1.47) 为最新正式版本，非草稿、非预发布；正文与双语 CHANGELOG 提取结果逐字一致。
- 已关闭测试浏览器并停止 10962 隔离宿主和回环模拟器，正式 Profile 未修改。隔离证据目录保留；没有真实平台端到端验收的新结论。
