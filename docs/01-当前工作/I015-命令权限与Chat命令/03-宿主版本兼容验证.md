# 宿主版本兼容验证

更新时间：2026-09-10 23:57（Asia/Shanghai）

## 当前补修

已按真实 0.1.5-rc.1 API 补修 setup(agentCtx, agent)、持久化列表 stat.header 和 tool/ptc-dispatch-start；保留 0.1.2-rc.1 的单参数 setup、平面 header 和旧事件名。c0945b0 的两个契约版本各 506 项通过、0 失败、0 跳过；本次历史恢复容错补修后各 508 项通过。尚未重跑真实宿主端到端，不能将回归通过视为完整兼容验收。详见 [04-回归与端到端验证](04-回归与端到端验证.md)。以下保留首轮评估和失效结论的历史依据。

## 首轮结论与范围

后续端到端验证发现 P1：真实 0.1.5-rc.1 宿主中新建会话触发 `cannot get property "agent" without inject`。以下自动化结果仍成立，但不能据此判定完整兼容；当前应以 [04-回归与端到端验证](04-回归与端到端验证.md) 的不通过结论为准。

用户提出验证 `0.1.0-rc.8 || 0.1.1-rc.2 || 0.1.2-rc.1 || 0.1.5-rc.1`。该表达式是有效的 npm semver 精确版本并集，但不能代表接口自动兼容。当前源码仅声明已通过本轮契约验证的 `0.1.2-rc.1 || 0.1.5-rc.1`，开发依赖固定至 npm latest 对应的 `0.1.5-rc.1`。

本轮未修改生产运行逻辑，未更新本机 DSH Profile；纳入 `chore: narrow DSH compatibility and update development baseline` 提交，未推送或发布。已发布的 0.1.41 元数据保持原样。下表是自动化与源码核对结论，不等同于真实渠道、网页布局及完整宿主生命周期联调。

| DSH 版本 | 实测结果 | 判断 |
| --- | --- | --- |
| 0.1.0-rc.8 | 真实 Connection 类没有 `requestRejection`；插件管理 GET 路由实测返回 503；当版没有 `dsh-api-session-controller` 包 | 当前功能不兼容，不加入支持范围 |
| 0.1.1-rc.2 | 同上；旧版 `api-remotes` 是客户端 Remote 装配结构，没有当前 SessionController 入口 | 当前功能不兼容，不加入支持范围 |
| 0.1.2-rc.1 | 495 项通过、0 失败、0 跳过 | 保留支持 |
| 0.1.5-rc.1 | 更新契约夹具后 495 项通过、0 失败、0 跳过；最新版开发依赖编译通过 | 自动化通过；后续端到端新建失败，待修复 |

## 旧版断点

旧版真实 `HostConnectionService` 使用 RPC channel 的 trusted-host 注册机制，没有 `requestRejection` 和 `authenticatedUrl`。将已有管理认证测试指向这两版，各出现 11 项失败、1 项通过、1 项跳过；其中多项失败发生在测试登录准备阶段，不能当成 11 个独立产品缺陷。

另行使用真实旧版 Connection 原型走插件管理 handler，确认 `/api/dsh-im-connect/channels` 返回 503。此结果与 `manager.ts` 缺少认证服务时关闭管理入口的逻辑一致，不能通过取消校验来实现兼容。

旧版 npm 未发布同版本 `dsh-api-session-controller`，而本插件的模型、历史、队列、会话接续等调用当前 `sessionController` 公共服务。回兼需要独立的旧版会话控制适配，以及保留等价认证边界的管理入口；单改 peer 范围无法完成。

## 最新版差异

首轮 0.1.5-rc.1 结果为 492 项通过、3 项失败，均来自契约夹具跟不上宿主内部依赖：

1. `SessionCommandController.prompt()` 新增 `agent.inbox.nextTurn/nextStep` 与 `session.snapshotEvents()` 去重检查，旧夹具没有这些字段。
2. 图片准入从 `dsh-attachment` 导出的自由函数迁移至 `AttachmentStore.admitPromptContent()`，旧夹具只提供 `saveImages()`。
3. prompt 路径新增 `fileUploads.bindPrompt()`，即使只含图片也执行回执绑定。

测试公共夹具补齐空输入队列，并按实际宿主导出选择真实附件准入函数或方法，继续覆盖图片持久化重开、拒绝不支持图片的模型、串行切模。文件服务夹具断言回执列表为空；文件上传本身仍不在这些测试的覆盖范围。

插件始终调用 `sessionController.prompt()`，没有直接依赖这些变化的内部接口，首轮据此未增加生产版本分支；后续真实创建生命周期出现注入错误，是否需要版本分支须经修复定位后判断。源码核对了会话控制所用 12 个方法、Agent 创建与恢复选项、标题的 `user/provider/fallback` 来源；未发现新的调用断点。

## 工程调整与验证

- 所有 DSH peer 依赖使用精确并集 `0.1.2-rc.1 || 0.1.5-rc.1`；开发依赖为 `0.1.5-rc.1`，不锁到旧版。
- 同步 lockfile；非 DSH 包没有版本升级。npm 在已有旧版 peer 树上直接更新发生 ERESOLVE，因此在隔离目录重新解析 DSH 节点，保留其他锁定版本；未使用 `--force` 或 `--legacy-peer-deps`。
- 全新隔离目录 `npm ci --ignore-scripts` 成功；工作区最新版依赖构建成功。
- CI 改为两个支持版本的矩阵，复用全部测试，保留全部契约检查；当前只完成本地矩阵验证，远程 CI 等提交后运行。
- 隔离安装曾遇镜像源解析缓慢，改用官方 npm registry 后完成；两个旧版缺包结论以实际 npm 发布记录和源码为准。

复现单个支持版本（在仓库根目录 PowerShell 执行，使用独立临时目录，不操作用户 Profile）：

```powershell
[Console]::OutputEncoding = [System.Text.Encoding]::UTF8
$OutputEncoding = [System.Text.Encoding]::UTF8
$dshVersion = '0.1.5-rc.1' # 或 0.1.2-rc.1
$contractDir = Join-Path $env:TEMP "dsh-im-contract-$dshVersion"
npm install --prefix $contractDir --registry=https://registry.npmjs.org --no-audit --no-fund "@deepseek-ai/dsh-api-session-controller@$dshVersion" "@deepseek-ai/dsh-commands@$dshVersion" "@deepseek-ai/dsh-attachment-local@$dshVersion" "@deepseek-ai/dsh-client-connection@$dshVersion"
if ($LASTEXITCODE -ne 0) { throw '契约依赖安装失败' }
$contractPackages = Join-Path $contractDir 'node_modules\@deepseek-ai'
$env:DSH_CHAT_CONTRACT_ROOT = Join-Path $contractPackages 'dsh-api-session-controller'
$env:DSH_ATTACHMENT_CONTRACT_ROOT = Join-Path $contractPackages 'dsh-attachment-local'
$env:DSH_CONNECTION_CONTRACT_ROOT = Join-Path $contractPackages 'dsh-client-connection'
npm test
```

## 待验证

维护者在真实 0.1.5-rc.1 Profile 验证登录与设置页、`/new` 后旧会话重开、工作区切换、图片输入、工具审批、归档恢复与重启保留。钉钉手机排版仍需真机确认。自动化结果不代表这些场景已经完成。

## 工具审批快照补修

真实 Session 探针确认 0.1.2-rc.1 和 0.1.5-rc.1 均不提供 events 属性，原审批读取会退回网页处理。现优先调用 snapshotEvents()，仅旧宿主回退 events；按 callId/subCallId 取最后匹配事件，不修改快照。

新增 3 项真实 Session 回归，修复前均失败，修复后覆盖普通工具、旧 code-dispatch 和新版 ptc-dispatch 的参数展示、排除其他调用参数、IM 批准回执及历史不变。两个版本各 506 项通过、0 失败、0 跳过。测试使用各版本真实 Session 和本地模拟渠道，不等于真实手机端到端验收。未安装正式 Profile、未提交、未推送、未发布。

本轮兼容、审批及弹窗补修纳入 `fix: adapt host session lifecycle and restore IM approvals` 提交，包含上述回归与端到端证据；未推送、未发布。前文“未提交”描述为各次验证时状态。

## 0.1.42 已发布

历史恢复现逐条校验，记录并跳过无法识别的条目；有效历史继续恢复且保持幂等。删除判定仍在无法确认时保留索引。两版新增坏条目混合恢复测试先失败后通过，全量各 508 项通过、0 失败、0 跳过。此前 503 为补修中间结果，495 对应 a1c1a40，506 对应 c0945b0。已恢复两个文件的纯排版改动，沿用仓库既有格式。

版本 0.1.42 已提交并推送，发布提交为 `90fe214`；GitHub 两版测试矩阵、npm 发布、双语 Release 同步均成功，npm gitHead 与发布提交一致。中英文发行说明只写用户相关内容。真实宿主新建→切换工作区→重启恢复及真实设置页仍未完成修复后端到端复测，不以本次自动化结果替代。此前“未提交/未发布”为对应阶段状态。
