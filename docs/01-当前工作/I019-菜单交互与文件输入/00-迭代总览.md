# I019 菜单交互与文件输入

状态：进行中

负责人：Codex；维护者负责真人渠道验收

优先级：P1

更新时间：2026-09-12 13:52（Asia/Shanghai）

## 交付重发策略与测试补强

企微普通正文、流式收口和交互说明共用过期回调判据：仅明确 `846605` 才主动推送，超时或其他错误原样上抛，不自动重发全文。修正完整说明成功但卡片失败时的回调清理注释，以及主动推送后误报回调成功的日志。删除正常完成早退后的不可达文案和翻译词条，飞书各发送入口统一数字／字符串零码判断。

验证：先复现旧代码在未知错误下仍回退的问题，再完成回归。全量 742 项：717 通过、0 失败、25 项宿主契约环境未配置而跳过。覆盖普通／流式正文的明确过期与未知错误、飞书正文／卡片／更新／反应／文件业务码、正文一次与文件一次且无完成文字、取消通知文字及重复结束去重。

关联提交：本次 `fix: prevent uncertain WeCom reply retries and unify delivery checks`。未安装、未重启实例、未推送。按钮后的主动正文可见性及流式收口后再次 Ask 的完整说明仍需维护者手机验收；本轮无数据迁移，可撤回本节对应源码、测试及构建产物恢复旧行为。

## 取消自动完成导航与企微按钮回调补修

用户实测每次应答追加完成说明、编号卡片和失效提示，要求改善体验。当前行为调整为所有渠道正常交付后不追加完成导航；错误、停止、空结果或交付失败仅发送必要文字，保留原始流式正文。主动菜单与必须回答的审批／问答保留，成功交付记账和消息状态不受影响。

本机日志确认企微返回 `846605 invalid req_id`。按钮事件帧不再入普通回复 FIFO，仅用于原卡片更新；后续操作回复走主动发送。完整说明遇到明确的 846605 拒绝可改走主动发送，超时等未知结果不自动重发，避免重复正文。

验证：成功回复无附加消息、异常提示、交付记账、按钮事件回复路径以及明确拒绝／未知回执回归通过。`npm test` 共 728 项，703 通过、0 失败、25 宿主契约用例跳过；构建及差异检查通过。更新本地测试包但不重启用户进程，真人企微点击与客户端展示仍需复测。关联提交：本次 `fix: suppress completion cards and correct WeCom callback handling`；未推送。此前各节的自动完成导航描述为历史行为，以本节为准。

## 原生频道入口与企微内容完整性

用户实测原生侧栏只有任务／定时页签，企微问题卡片的正文与按钮出现省略号。本轮原生侧栏改为优先读取宿主 `entriesOfSlot` 的生效项，兼容旧 `entries`；对其他插件直接替换组件但未触发槽位通知的加载顺序，最多重试 20 次、每次间隔 250ms，卸载时清理。仍复用现有任务树与页签注册表，不依赖 Codex UI。

企微交互先投递完整说明和编号选项，再发送紧凑编号按钮卡；不再把长问题放进卡片副标题或截断按钮语义。超过文字上限沿用文字分片降级；完整说明已成功但按钮失败时不重复正文，不残留其对应回调。未知消息 ID 仍不猜测队列位置。

验证：新增槽位投影、全文先于卡片、长问题／长选项、卡片失败清理回归；`npm test` 为 725 项，700 通过、0 失败、25 宿主契约用例跳过，构建与差异检查通过。agent-browser 无法连接现有 Chrome，未完成当前网页的实际渲染验收，也没有向真实企微发送消息。沿用本地测试安装授权更新包，但不重启用户进程；需用户自行重启并刷新后确认原生频道页签与手机内容完整性。关联提交：本次 `fix: restore native channel navigation and complete interaction delivery`；未推送。

## 评审补修与本地启动交接

用户要求停止预览后自行启动。本轮已停止此前创建的 3081 端口预览，不再启动宿主或向真实渠道发送消息。

- 修复初始化异步链：初始化失败不再无条件恢复会话，后台异常统一记录到文件；管理器卸载后停止后续服务读取，会话索引恢复在异步边界检查引擎是否已释放。已复现并验证“初始化等待期间卸载”回归；此前实际启动的 `permissionPresets ... inactive context` 是否完全解除仍需用户启动确认，不能把生命周期单测视为真实启动通过。
- 企微审批／问答携带原任务输入或已验证交互输入的消息 ID；只保存 ID，不保留附件字节。数字 ID 仅在安全整数范围内转换，入站与回调匹配同规则；未知 ID 不猜测队列位置。审批和多选问答卡片回归验证消息 ID 的传递，原有迟到导航保护继续通过。
- 飞书媒体读取失败提示补业务码检查；失败日志不包含接口敏感详情。正文兼容数字与字符串零码。
- QQ 在既有 HTTP／鉴权检查之外，对响应明确包含的非零业务码报告失败；这是防御性校验，不作为线上已经返回过 HTTP 200 业务错误的证据。
- 钉钉对空或非 JSON 响应给出稳定错误，兼容字符串零码；保留“取消未发送增量、等待在途更新、最终全文覆盖”的收口语义。

验证：最终 `npm test` 共 721 项，696 通过、0 失败、25 宿主契约用例因环境未配置而跳过。构建产物同步；关联提交：本次 `fix: restore native channel navigation and complete interaction delivery`；未推送。本地 web Profile 已安装 `im-connect-review-20260912` 目录内的 0.1.45 测试包，249 个 lib 文件哈希与仓库一致。收尾时配置为 `[]`，没有禁用插件；3081 出现另一个 PID 33096，本轮没有创建或停止它。由用户自行重启并验收“菜单／新会话 → 普通提问 → 正文 → 完成导航”和审批／问答路径。无持久化格式迁移，可回退代码与本地包。

## 跨渠道正文交付补修

用户要求检查其他渠道后，进一步授权修复。本次保留企微修复，并修正以下已复现问题：

- 钉钉：增量更新串行执行，最终全文等待在途更新结束后再写入；普通正文和卡片失败后的文字降级共用 webhook 发送函数，校验 HTTP 状态与 `errcode: 0`，业务拒绝或无效回执向上层报告失败。
- 飞书／Lark：检查正文发送的业务错误码，SDK 返回非零 `code` 时不再视为交付成功。
- Telegram：首段更新原气泡，剩余正文按 Unicode 字符每段最多 4000 字依次发送，避免截断或拆开 emoji；尾段失败向上层传播，不发送成功导航。

新增 `tests/channel-delivery-regressions.test.mjs`，九个用例均先复现失败再通过，覆盖钉钉在途更新成功／失败、两种文字发送入口的业务拒绝、飞书与 Lark 真实 SDK 模拟错误回执、Telegram 长回复与编辑降级及尾段失败。钉钉既有成功响应夹具补齐正式 `errcode: 0` 字段，原换行、列表和代码保留断言不变。

最终 `npm test`：713 项，688 通过、0 失败、25 跳过（未配置真实宿主契约环境）；构建和 `git diff --check` 通过。源码与构建产物同步。关联提交：本次 `fix: preserve channel reply delivery and completion order`。未安装正式实例、未发送真实渠道消息、未推送或发布。维护者仍需安装后验收各客户端正文完整显示及完成导航顺序；回退本次代码差异即可，无配置或持久化格式变化。

## 企业微信卡片后的正文回调修复

2026-09-12 用户截图显示仅有思考提示和完成导航。对应本机日志记录本次生成并调用回调投递 414 字正文，但客户端截图未显示正文，不能把渠道 API 成功等同于客户端呈现成功。

已复现菜单卡片主动推送后原命令回调仍留在 FIFO，下一条正文会取走旧回调。现由 `WecomReplyBroker.sendCard` 统一发送卡片，成功后仅移除原消息 ID 对应的回调；完成导航不消费后续输入，卡片失败保留原回调供文字降级。未改变正文传输协议或新增依赖。

验证：先在保持旧发送行为的用例中确认回调残留断言失败，再修复；新增三项回归覆盖下一条正文、迟到导航及卡片失败降级。`npm test` 为 679 通过、0 失败、25 跳过（未配置真实宿主契约路径）。`git diff --check` 通过。修改包含源码及同步构建产物，关联提交：本次 `fix: preserve channel reply delivery and completion order`。

待处理：维护者安装更新并在企业微信重测“菜单／新会话 → 普通提问 → 完成导航”。本轮未安装正式 Profile、未发送真实渠道消息、未推送或发布。代码可回退本次差异，无持久化数据迁移。

## 目标

分三部分实现并分别提交：操作菜单、审批与问答按钮、通用文件输入。复用现有准入和命令权限、Host 命令及 Chat 附件契约；不安装正式 Profile、不推送和发布。

## 一、操作菜单

已实现 `/menu`、`/m` 及会话、工作区、模型分页选择、新建、停止、导出、状态、帮助。服务端保存动作，随机 token 绑定账号、聊天、操作者、会话，15 分钟有效，一次性消费；执行重新检查权限。普通文字退出数字菜单，问答优先。

Telegram、飞书/Lark 原生按钮；企业微信不超过 6 项时使用原生卡片，否则文字降级。钉钉、QQ、微信为文字序号菜单；未引入需要额外发布的自有钉钉交互模板。菜单与问答的具体呈现范围以 README 为准。

构建通过，三个兼容版本各 581 项通过、0 失败、0 跳过。原生请求及统一隔离有自动化覆盖，真实渠道卡片渲染、回调权限仍未验证。

关联提交：本次 `feat: add scoped interactive command menus`。

## 二、审批与问答

已接入原生按钮适配渠道；批准一次/拒绝、单选和多选提交复用既有 Host 答案结构。原操作者、会话和具体问题/审批请求身份重新校验；原生按钮失效或无法完整展示时沿用文字提示，不缩减审批参数。工具审批仍仅授权私聊用户可处理；命令关闭不影响已存在的交互。提示和按钮提供中英文。

三版各 583 项通过、0 失败、0 跳过；新增审批身份、旧按钮重放、多选显式提交回归。真实卡片渲染和回调尚未验证。关联提交：本次 `feat: add approval and question action buttons`。

## 三、文件输入

微信、企微、钉钉、飞书/Lark、QQ、Telegram 接收普通文件，保留下载地址校验、加密解密、大小限制和原消息上下文。微信文件直接传递字节，不再将缓存路径注入模型。引擎通过 `fileUploads.uploadStream` 上传，再使用 `sessionController.prompt` 提交 `type: file, receiptId`，不自建文件解析体系。

每条消息最多 4 个普通文件、累计 20 MiB，渠道下载上限可能更低；上传限时 120 秒并传递生命周期信号。文件说明文字不作为命令。文件能否被模型/工具解析与 Chat 一致。

两个 0.1.5 版本支持完整文件上传；0.1.2-rc.1 没有这套 Chat 契约，明确提示升级，不能称为旧版支持文件输入。未新增生产依赖。

三版完整回归各 603 项通过、0 失败、0 跳过。新版使用真实 FileUploads 和 SessionCommandController 验证凭据与最终 file attachment；旧版核对契约缺失与拒绝行为。全部渠道有普通文件接收自动化覆盖；下载错误、超限、说明文字、上传中停用账号有回归。顺带验证飞书原生卡片业务错误能触发文字降级。

关联提交：`29131c1 feat: accept channel files through Chat upload admission`。已安装本机 web Profile，版本号仍为 0.1.43；未推送、未发布。

本地安装：使用 DSH CLI 安装独立路径的本地包，240 个产物与工作区 SHA256 一致，Profile 配置组合成功且包含 IM 插件。pnpm 单独检查报告宿主提供的 peer 缺失，未为此向 Profile 重复安装宿主依赖。尚未启动宿主验证运行时加载，维护者重启 DSH 并刷新页面后进行真实渠道测试；未移动插件目录或备份用户数据。

## 验收与恢复

五项卡片补齐已完成开发并逐项提交，详见[卡片交互补齐计划](01-卡片交互补齐计划.md)。完整回归 630 项：607 通过、0 失败、23 跳过。另完成卡片回调语言补修，选择模块 11 项全通过。新增钉钉原生按钮取代此前文字导航限制；旧段落记录的是当时状态。安装记录仅对应此前本地构建，新增交互尚未安装、真实渠道待验收。

命令导航补齐（本次 `feat: connect command replies with contextual navigation`）：常用命令结果共用相关入口，原生按钮渠道可点击进入选择菜单和查询，文字渠道显示对应命令；结果导航不接管数字消息。菜单按渠道容量分页，企业微信主菜单也可翻页，子菜单显示当前位置、空态和返回入口；卡片不重复长会话 ID，文字降级保留完整命令及正文。命令结果发送失败不重复执行操作，按钮绑定操作后的当前会话。中英文 README 和文案同步。完整测试 612 项：589 通过、0 失败、23 项未配置真实宿主契约环境而跳过。追加切换后导航绑定回归通过，命令测试 43 项：42 通过、0 失败、1 跳过。导航改动尚未重新安装，未推送、未发布。尚未验证手机端卡片显示；钉钉本轮仍为文字导航，未新增原生按钮模板与回调。

本地联调补修（`927b251`）：钉钉同一回合的 `write` 绝对路径与 `present` 相对路径导致重复文件回传。现在以会话工作区解析的绝对路径去重，原路径仍交给 Chat 文件服务校验。回归先复现 3 次发送，再验证仅 1 次；不同目录同名文件与下一回合再次交付仍正常。构建通过，文件回传、渠道发送和 gateway 投递测试共 37 项：36 通过、0 失败、1 项真实宿主测试因未配置契约环境跳过。修复已更新到本机 web Profile，手机复测待维护者完成。

维护者使用真实账号验收菜单、按钮回调、文件收发。无数据迁移，恢复上一版本撤回功能；已发送的卡片在重启后失效，远端消息不会随回退删除。

阅读顺序：本总览 → README 操作菜单 → src/engine/choices.ts。

## 预设命令补齐

`/presets`、`/presetlist`、`/preset` 已实现目录、当前预设、数字 ID、默认选择和分页按钮。通过宿主 create({ workspaceId, agentPreset }) 新建并接入当前聊天，不修改账号默认；旧会话保留。创建后接续失败返回可恢复的新会话 ID。宿主仅允许空会话就地切换预设，本期采用一致的新建语义。新增四项行为回归通过；真实渠道待统一验收。关联提交：`cc8659f feat: add Agent preset selection commands`。

## 推理选择补齐

`/reasoning` 与别名支持原生分页、等级序号、完整 ID 和恢复默认。选择凭据绑定账号、聊天、用户、会话和模型，15 分钟有效、一次消费，防止旧按钮改错模型；无等级时不提供修改按钮。新增五项行为回归，含数字参数不能误作翻页。关联提交：`05727b3 feat: add model-bound reasoning choices`。真实渠道待统一验收。

## 模型、推理与预设导航

三类命令回复互相提供入口，单页选择卡片也提供相关入口；分页卡片保留翻页和返回菜单，遵循渠道按钮容量。文字降级显示易读指令，内部选择凭据不进入展示。中英文帮助与 README 操作菜单同步。关联提交：本次 `feat: connect model reasoning and preset navigation`。

验证：三个支持版本（0.1.2-rc.1、0.1.5-rc.1、0.1.5-rc.2）各 643 项通过、0 失败、0 跳过。新增真实 SessionCommandController 创建预设及工作区归属失败测试；这是隔离契约证据，不代表手机端验收。首次运行的文件/导出测试路径少了 node_modules/@deepseek-ai，修正测试环境路径后全部通过，未改相关生产代码。

本轮三项开发已完成并逐项提交；未安装、未推送、未发布。维护者统一验收：预设按钮新建且旧会话保留、恢复默认、推理等级与模型切换后的旧按钮、中英文回复、企微分页与文字降级。没有账号配置或数据迁移，可通过撤回对应功能提交恢复；已创建的会话继续保留。

## 任务结束通知与结果导航

已完成。仅扩展 IM 消息认领回合的结束反馈，复用请求 ID / rpcId 关联与正文、文件投递等待；成功、空结果、执行失败、停止和投递失败分开提示。不把回合完成宣称为长期目标已经达成。审批、问答继续使用原交互入口，不增加重复等待通知。

结果导航提供最近记录、状态、支持文件发送时的会话导出及菜单。命令关闭时只发文字；导航不接管纯数字输入。卡片点击复用现有准入、权限与会话绑定校验。已存在的流式失败卡片直接收口为文字导航，不再重复发结束卡片；不重试任务或文件。重复结束事件去重，停用、换绑、等待交互或后续回合开始后抑制旧通知。

验证：新增 8 项回归；0.1.5-rc.2 隔离契约环境完整测试 651 项通过，0 失败、0 跳过。覆盖通知等待投递、重复事件、停用后迟到结果、命令关闭、换绑、等待交互、新回合覆盖和流式失败收口。真实手机端通知与卡片效果待维护者统一验收。未安装、未推送、未发布。本期未实现批量输入或语音输入。关联提交：本次 `feat: add turn completion notices and result navigation`。

无数据迁移，回退本次提交即可撤回新增通知；已发送的远端提示不会随回退删除。

## 帮助正文与导航布局

帮助正文按普通消息发送并保留分区空行，底部独立短导航卡片。企微与钉钉提供相同操作；普通导航卡片不重复列出命令，文字降级仅提示发送命令，不提示点击。分页根据实际导航占位，首页不空留上一页位置。完整测试 655 项：631 通过、0 失败、24 项未配置宿主契约环境跳过。手机端分区间距和按钮点击仍待复测；此前钉钉 unknown-action 回调补修独立保留，不以布局测试宣称点击已验证。关联提交：本次 fix: separate help content from compact navigation cards。

帮助呈现调整：按维护者最新要求，所有渠道 `/help` 仅发送文字正文，不再附加导航卡片或底部导航清单；正文保留 `/menu` 使用入口。撤除上一轮帮助专用拆分投递和闲置翻译键。命令测试 63 项：61 通过、0 失败、2 项宿主契约未配置跳过。关联提交：本次 `fix: render help as text across all channels`。


## 钉钉回调参数补修

回调先验证卡片身份，再从该卡片已发送的令牌集合解析动作。支持直通 actionIds、模板 actionIds + params、仅 params 三种格式；多个不同合法令牌冲突时拒绝，不把参数解释成命令。保留操作者、卡片、时效检查，解析失败日志使用 unresolved-action 且仅记录结构。渠道级测试覆盖三种成功路径，纯函数测试覆盖跨字段冲突。关联提交：本次 `fix: resolve DingTalk card actions from scoped parameters`。真实手机回调待维护者复测。
