# 主动投递接口（已撤回）

更新时间：2026-09-07 16:00（Asia/Shanghai）

当前工作区已撤回主动投递实现，不再注册 `im-send`、`im_accounts`、`im_targets`、`im_target_save`、`im_send`，也不提供 `/dsh-im-connect/api/delivery/*` HTTP 路由。调用方不得依赖此前未完成的接口。

原有账号配置、IM 回复与插件更新接口保留。没有迁移或删除真实状态目录；旧投递字段不再被本插件读取执行。

历史实现见提交 `9028bac`；撤回范围与验证见 [I013 归档](../07-迭代归档/2026/I013-主动投递/00-迭代总览.md)。
